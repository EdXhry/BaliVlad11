# 🛠️ Панель управления по ТЗ

## Запуск панели

```bash
# В отдельном терминале
py -m uvicorn panel:app --host 0.0.0.0 --port 8000

# Или напрямую
python panel.py
```

После запуска откройте в браузере: http://localhost:8000

## Что реализовано по ТЗ

### ✅ Основное ТЗ

1. **Фильтрация по тематике** (`config.py`):
   - Включение: только инвестиции/недвижимость/property/real estate/development
   - Исключение: вебинары/стримы/юриспруденция/миграция/ВНЖ

2. **Гео-фильтр Бали** (`processor.py`):
   - Оффлайн: только районы Бали (Denpasar/Canggu/Ubud/Seminyak/Sanur/Kuta/Jimbaran/Uluwatu/Nusa Dua/Bukit)
   - Онлайн: без гео-ограничений

3. **Языковая фильтрация**:
   - Оффлайн: RU или EN
   - Онлайн: только RU

4. **Структурированный вывод** (`digest.py`):
   ```
   📅 25 июня 2025, 10:00 WITA
   📍 Seminyak, Bali
   🏷 ОФФЛАЙН | Тип: конференция
   🧵 Инвестиции в коммерческую недвижимость на Бали
   🗣 Иван Петров (RealEstateBali)
   Язык: 🇷🇺🇬🇧 RU+EN
   🔗 [Подробнее](ссылка)
   ```

5. **Панель управления** (`panel.py`):
   - Включение/выключение источников
   - Ключевые слова / Анти-слова
   - Расписание (время WITA)
   - Кнопка "Обновить сейчас"

### ⚠️ Что нужно доделать

1. **Прокси** — находится в `bot.py`, строка:
```python
PROXY_URL = os.getenv("PROXY_URL")  # http://user:password@ip:port
```
Нужно вписать рабочий прокси в `.env` или удалить строку.

2. **Реальные источники**:
   - Сейчас только тестовые данные (`TestRealEstateParser`)
   - Нужно добавить:
     - Telegram каналы (через Telethon/pyrogram)
     - Сайты по недвижимости на Бали

3. **Telegram парсинг**:
   - Реализовать `TelegramChannelParser` в `realestate_parser.py`
   - Нужен API ID/API Hash от Telegram

4. **Расписание WITA**:
   - Планировщик сейчас на UTC
   - Нужно настроить часовой пояс в `main.py`

5. **Панель улучшения**:
   - Сохранение конфигурации фильтров в реальный файл конфигурации
   - Добавление новых источников через UI

## 📋 Критерии приемки ТЗ

| Требование | Статус | Где реализовано |
|------------|--------|-----------------|
| 10-20 событий/неделю | ⚠️ Тест | `TestRealEstateParser` |
| 0 оффлайна вне Бали | ✅ | `processor.py:is_bali_location()` |
| 0 онлайн не на RU | ✅ | `processor.py:detect_language()` |
| Кнопка "Обновить сейчас" | ✅ | `panel.py:run_now()` |
| Панель управления | ✅ | Веб-интерфейс на FastAPI |
| Расписание 09:00 WITA | ⚠️ UTC | `main.py` (нужна настройка) |
| Структурированный вывод | ✅ | `digest.py:_format_event_tz()` |

## 🚀 Следующие шаги

1. **Установить зависимости**:
```bash
py -m pip install -r requirements.txt
```

2. **Создать БД PostgreSQL**:
```bash
createdb -U postgres bali_events
```

3. **Тестировать базовый функционал**:
```bash
python main.py test-bot      # Проверить бота
python main.py collect       # Запустить сбор
python main.py publish       # Опубликовать
```

4. **Запустить панель**:
```bash
python panel.py
```

5. **Найти реальные источники** (Telegram + сайты)

## 📁 Изменения по сравнению с первоначальным

1. **Новые файлы**:
   - `config.py` — фильтры по ТЗ
   - `processor.py` — полная переработка под ТЗ
   - `digest.py` — структурированный вывод
   - `realestate_parser.py` — парсеры недвижимости
   - `panel.py` — веб-панель

2. **Измененные**:
   - `models.py` — добавлены поля по ТЗ
   - `collector.py` — использует новые парсеры
   - `main.py` — расписание по ТЗ

3. **Старые парсеры**:
   - `parsers/html_parser.py` — для общих мероприятий (не под ТЗ)
   - `parsers/api_parser.py` — Eventbrite API

## 🔧 Где менять прокси

1. **Telegram бот**: `bot.py`, строка 23
2. **HTTP парсеры**: `parsers/base.py`, строка 38 (клиент `httpx`)

Если не нужен прокси — удалите строку `PROXY_URL` из `.env` и `bot.py`.
